require "spec_helper"

RSpec.describe A1notationRb do
  it "has a version number" do
    expect(A1notationRb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
